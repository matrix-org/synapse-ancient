Installation
------------

To install the synapsekeyserver module run::

    $ python setup.py install


Basic Usage
-----------

Generate config::

    $ python -m synapsekeyserver.config -c keyserver.cfg --bind-port=8081

Run server::

    $ python -m synapsekeyserver.keyserver -c keyserver.cfg
